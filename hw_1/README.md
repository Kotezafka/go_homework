## Задание

Напишите программу, которая выполняет следующие действия:

1. Выводит **имя пользователя** из переменной окружения
2. Считывает **аргументы командной строки (CLI)** и выводит их
3. Показывает текущую **версию Go**, используя `runtime.Version()`